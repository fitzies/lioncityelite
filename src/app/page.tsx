export const metadata = {
  title: "Lion City Elite",
  description: "",
};

const Page = ({ params }: { params: { slug: string } }) => {
  return (
    <div className="w-screen h-screen bg-[#171717]">
      <h1 className="font-bold text-4xl">Website in progress</h1>
    </div>
  );
};

export default Page;
